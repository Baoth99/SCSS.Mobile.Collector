import 'package:flutter/material.dart';

class StatisticLayout extends StatelessWidget {
  const StatisticLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('StatisticLayout'),
    );
  }
}
